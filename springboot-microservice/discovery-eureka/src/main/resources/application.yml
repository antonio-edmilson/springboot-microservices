spring:
  application:
    name: discoviry
 
server:
  port: 9080
  
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://discovery-eureka:${server.port}/eureka/