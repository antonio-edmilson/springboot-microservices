server:
  port: 9091

spring:
  application:
    name: sale
  jpa:
    hibernate:      ddl-auto: update
    properties:      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3307/saledb?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: root
    
eureka:
  instance:
    prefer-ip-address: true
    appname: sale
  client:
    service-url:
      defaultZone: http://localhost:9080/eureka/
    register-with-eureka: true
    fetch-registry: true